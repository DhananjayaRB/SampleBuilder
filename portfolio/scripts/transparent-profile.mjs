/**
 * Disabled — automatic background removal was harming the portrait quality.
 * Swap public/profile.png yourself (e.g. export from a photo editor) if you need changes.
 */
console.log('transparent-profile.mjs: no-op (script disabled).')
process.exit(0)
